using Microsoft.EntityFrameworkCore;
using Volo.Abp;

namespace FS.Abp.Emailing.EntityFrameworkCore;

public static class EmailingDbContextModelCreatingExtensions
{
    public static void ConfigureEmailing(
        this ModelBuilder builder)
    {
        Check.NotNull(builder, nameof(builder));

        /* Configure all entities here. Example:

        builder.Entity<Question>(b =>
        {
            //Configure table & schema name
            b.ToTable(EmailingDbProperties.DbTablePrefix + "Questions", EmailingDbProperties.DbSchema);

            b.ConfigureByConvention();

            //Properties
            b.Property(q => q.Title).IsRequired().HasMaxLength(QuestionConsts.MaxTitleLength);

            //Relations
            b.HasMany(question => question.Tags).WithOne().HasForeignKey(qt => qt.QuestionId);

            //Indexes
            b.HasIndex(q => q.CreationTime);
        });
        */
    }
}
